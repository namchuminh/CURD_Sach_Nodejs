<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="<%= baseUrl %>/css/site_detail.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Đặt Hàng</title>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="<%= baseUrl %>">Trang Chủ</a>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="<%= baseUrl %>/quan-ly/">Quản Lý <span class="sr-only"></span></a>
                    </li>
                    <% if (user) { %>
                        <li class="nav-item active">
                            <a class="nav-link" href="<%= baseUrl %>/don-hang/">Đơn Hàng <span class="sr-only"></span></a>
                        </li>
                    <% } %>
                    <li class="nav-item active">
                        <% if (user) { %>
                            <a class="nav-link" href="<%= baseUrl %>/dang-xuat/">Đăng Xuất <span class="sr-only"></span></a>
                        <% } else { %>
                            <a class="nav-link" href="<%= baseUrl %>/dang-nhap/">Đăng Nhập <span class="sr-only"></span></a>
                        <% } %>
                    </li>
                    <% if (!user) { %>
                        <li class="nav-item active">
                            <a class="nav-link" href="<%= baseUrl %>/dang-ký/">Đăng Ký <span class="sr-only"></span></a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container" style="margin-top: 80px;">
        <div class="row">
            <div class="col-md-6 col-sm-6">
                <div class="card text-dark bg-light">
                    <div class="card-header">
                        Thông Tin Đặt Hàng
                    </div>
                    <form action="<%= baseUrl %>/dat-hang/" method="POST">
                        <div class="card-body">
                            <div class="mb-3">
                                <label for="khachhang" class="form-label">Khách Hàng: </label>
                                <input type="text" class="form-control" id="khachhang" disabled value="<%= user.dataValues.hoTen %>">
                                <div id="emailHelp" class="form-text">Thông tin được sử dụng khi bạn đăng ký tài khoản.</div>
                            </div>
                            <div class="mb-3">
                                <label for="email" class="form-label">Email</label>
                                <input type="text" class="form-control" id="email" disabled value="<%= user.dataValues.email %>">
                            </div>
                            <div class="mb-3">
                                <label for="diachi" class="form-label">Địa Chỉ: </label>
                                <input type="text" class="form-control" id="diachi" name="diaChi" placeholder="Nhập địa chỉ giao hàng..." required>
                            </div>
                        </div>
                        <input type="text" hidden name="book_id" value="<%= book.dataValues.id %>">
                        <input type="text" hidden name="number" value="<%= number %>">
                        <div class="card-footer">
                            <button type="submit" class="btn btn-primary">Đặt Hàng</button>
                        </div>
                    </form> 
                </div>
            </div>
            <div class="col-md-6 col-sm-6">
                <div class="card mb-3">
                    <div class="row g-0">
                      <div class="col-md-4">
                        <img src="<%= baseUrl %>/uploads/<%= book.dataValues.anhBia %>" class="img-fluid rounded-start" alt="...">
                      </div>
                      <div class="col-md-8">
                        <div class="card-body">
                          <h5 class="card-title"><%= book.dataValues.tieuDe %></h5>
                          <hr>
                          <p class="card-text">Tác giả: <%= book.dataValues.tacGia %></p>
                          <p class="card-text"><small class="text-muted">Số trang: <%= book.dataValues.soTrang %> trang</small></p>
                          <p class="card-text"><small class="text-muted">Ngày phát hành: <%= book.dataValues.ngayPhatHanh %></small></p>
                          <p class="card-text"><small class="text-muted">Số lượng mua: <%= number %> cuốn</small></p>
                        </div>
                      </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>