<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="<%= baseUrl %>/css/site_index.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Trang Chủ</title>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="<%= baseUrl %>">Trang Chủ</a>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="<%= baseUrl %>/quan-ly/">Quản Lý <span class="sr-only"></span></a>
                    </li>
                    <% if (user) { %>
                        <li class="nav-item active">
                            <a class="nav-link" href="<%= baseUrl %>/don-hang/">Đơn Hàng <span class="sr-only"></span></a>
                        </li>
                    <% } %>
                    <li class="nav-item active">
                        <% if (user) { %>
                            <a class="nav-link" href="<%= baseUrl %>/dang-xuat/">Đăng Xuất <span class="sr-only"></span></a>
                        <% } else { %>
                            <a class="nav-link" href="<%= baseUrl %>/dang-nhap/">Đăng Nhập <span class="sr-only"></span></a>
                        <% } %>
                    </li>
                    <% if (!user) { %>
                        <li class="nav-item active">
                            <a class="nav-link" href="<%= baseUrl %>/dang-ky/">Đăng Ký <span class="sr-only"></span></a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container" style="margin-top: 80px;">
        <div class="row" id="products">

            <% books.forEach((book,index) => { %>
                <div class="col-md-3 col-sm-3 mb-3">
                    <div class="card h-100">
                        <img src="<%= baseUrl %>/uploads/<%= book.anhBia %>" class="card-img-top"
                            alt="Dune">
                        <div class="card-body">
                            <h5 class="card-title"><%= book.tieuDe %></h5>
                            <p class="card-text"><%= book.moTa %></p>
                            <p><span>Tác giả: <%= book.tacGia %></span></p>
                        </div>
                        <a class="btn btn-danger w-100" href="<%= baseUrl %>/san-pham/<%= book.id %>" role="button">XEM CHI TIẾT</a>
                    </div>
                </div>
            <% }) %>


        </div>
    </div>
</body>

</html>